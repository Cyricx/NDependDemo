@model IEnumerable<NDependTestToDos.ToDoCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ToDoCategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ToDoCategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ToDoCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ToDoCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ToDoCategoryID })
        </td>
    </tr>
}

</table>
